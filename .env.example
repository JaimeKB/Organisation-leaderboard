# Classic GitHub Personal Access Token, created under your own account at
# https://github.com/settings/tokens (not the org's settings — you don't
# need to own or administer an org to read data you already have access to).
# Required scopes: read:org, repo
GITHUB_PAT=

# Port the app listens on, both inside the container and on localhost.
PORT=8080
